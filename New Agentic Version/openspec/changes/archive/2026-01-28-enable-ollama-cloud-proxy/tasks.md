## 1. Configure Proxy

- [x] 1.1 Update `vite.config.ts` to include the `/ollama-cloud` proxy rule.

## 2. Verification

- [x] 2.1 Test cloud connection via `curl` through the proxy (requires dev server running or simple manual check).
- [x] 2.2 Verify local connection still works.
